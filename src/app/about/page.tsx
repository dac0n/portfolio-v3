import { MenuBtn } from "@/components/menu-btn/MenuBtn";
import { NavMenu } from "@/components/nav-menu/Nav-Menu";

export default function About() {
  return (
    <main className="flex h-screen w-screen flex-col items-center justify-center bg-black text-center align-middle">
      <NavMenu />
    </main>
  );
}
